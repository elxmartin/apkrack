# Ignore all plaintext report files inside public/reports
public/reports/**/*.json
public/reports/**/*.txt

# Explicitly keep encrypted reports
!public/reports/**/*.enc
# The access Worker is source code and must be version controlled.
!worker.js

# Ephemeral decrypted pipeline state; it must never be committed.
.pipeline-status.json
.dev.vars*
.wrangler/
